# -*- mode: sh; eval: (sh-set-shell "zsh") -*-

if homebrew_formula_exists zoxide; then
    export STARSHIP_CONFIG="$(xdg_config_for starship)/starship.toml"
    shellmgr_command_init starship init zsh
fi