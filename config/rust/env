# -*- mode: sh; eval: (sh-set-shell "zsh") -*-

if homebrew_formula_exists rustup; then
    # This runs on each shell to allow for toolchain swapping
    export RUST_SRC_PATH="$(rustc --print sysroot)/lib/rustlib/src/rust/src"
fi
