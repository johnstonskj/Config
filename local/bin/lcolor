#! /usr/bin/env zsh
# shellcheck shell=bash # -*- mode: sh; eval: (sh-set-shell "zsh") -*-

typeset -i col
for col in {0..255}; do
    printf "\x1b[38;5;${col}mcolor%-5i\x1b[0m" $col
    if ! (( ( col + 1 ) % 8 )); then
        echo
    fi
done